Two images from the held-out test split of the aquarium dataset, chosen with
the same md5(file stem) rule as scripts/prepare_data.py, so neither was
trained on. They are copied here from the Kaggle run's `artifacts/samples/`
(`tank_01.jpg`, `tank_02.jpg`); the README's curl examples use tank_01.jpg.
